<div class="container">
<p>
  <strong><span class="glyphicon icon-large glyphicon-tag">  郵件種類名稱:</strong>
  <%= @category.title %>
</p>
<%  if current_user.is_admin? %>
<%= link_to 'Edit', edit_category_path(@category) %> |
<% end %>
<%= link_to 'Back', categories_path %>
</div><br/>
